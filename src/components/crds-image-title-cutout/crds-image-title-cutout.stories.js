import { storiesOf } from '@storybook/polymer';


const stories = storiesOf('Personalization', module);
stories
  .add('<crds-image-title-cutout />', () => {
    return `<crds-image-title-cutout></crds-image-title-cutout>`;
  });
